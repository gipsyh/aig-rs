use crate::Aig;

impl Aig {
    pub fn simplify(&self) -> Aig {
        // let inputs: HashSet<usize> = self.inputs.iter().cloned().collect();
        // for l in self.latchs.iter() {
        //     if l.init.is_none() &&  l.next.node_id()
        // }
        todo!()
    }
}
